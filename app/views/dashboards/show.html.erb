<h1>You're logged in! Welcome to the User Dashboard!</h1>
<head>
  <p>
    <strong>W.I.P.</strong>
  </p>
</head>

<body>
  <% if current_user.is_admin? %>
    <p>Howdy <%= current_user.full_name %> as admin</p>
  <% else %>
    <p>Howdy <%= current_user.full_name %> as user</p>
  <% end %>
  <div>
    <%= link_to "Sign Out", destroy_user_session_path %>
  </div>
</body>
